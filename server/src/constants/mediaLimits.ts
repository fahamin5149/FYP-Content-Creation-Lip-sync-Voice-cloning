/** Max stored video templates per user (user_media, media_type = video). */
export const MAX_USER_VIDEOS = 10

/** Max stored voice samples per user **per language** (english / urdu). */
export const MAX_USER_AUDIOS_PER_LANGUAGE = 10
