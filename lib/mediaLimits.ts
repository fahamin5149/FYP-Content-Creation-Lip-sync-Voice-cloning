/** Mirror server limits — max stored video templates per user. */
export const MAX_USER_VIDEOS = 10

/** Max stored voice samples per language (English / Urdu) per user. */
export const MAX_USER_AUDIOS_PER_LANGUAGE = 10
